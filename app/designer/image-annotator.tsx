"use client";
import {MouseEvent,PointerEvent,useState} from "react";
export type ImageNote={x:number;y:number;x2?:number;y2?:number;text:string};
export type RenderMode="strict"|"annotated"|"redesign";
type Point={x:number;y:number};

export default function ImageAnnotator({image,notes,onChange,mode,onModeChange}:{image:string;notes:ImageNote[];onChange:(notes:ImageNote[])=>void;mode:RenderMode;onModeChange:(mode:RenderMode)=>void}){
 const[draft,setDraft]=useState(""),[tool,setTool]=useState<"point"|"line">("line"),[start,setStart]=useState<Point|null>(null),[end,setEnd]=useState<Point|null>(null);
 function point(clientX:number,clientY:number,target:HTMLDivElement):Point|null{const box=target.getBoundingClientRect(),img=target.querySelector("img");if(!img)return null;const scale=Math.min(box.width/img.naturalWidth,box.height/img.naturalHeight),width=img.naturalWidth*scale,height=img.naturalHeight*scale,left=box.left+(box.width-width)/2,top=box.top+(box.height-height)/2;if(clientX<left||clientX>left+width||clientY<top||clientY>top+height)return null;return{x:Math.round(((clientX-left)/width)*1000)/10,y:Math.round(((clientY-top)/height)*1000)/10}}
 function placePoint(event:MouseEvent<HTMLDivElement>){if(tool!=="point"||!draft.trim())return;const p=point(event.clientX,event.clientY,event.currentTarget);if(!p)return;onChange([...notes,{...p,text:draft.trim()}]);setDraft("")}
 function begin(event:PointerEvent<HTMLDivElement>){if(tool!=="line"||!draft.trim())return;const p=point(event.clientX,event.clientY,event.currentTarget);if(!p)return;event.currentTarget.setPointerCapture(event.pointerId);setStart(p);setEnd(p);event.preventDefault()}
 function move(event:PointerEvent<HTMLDivElement>){if(!start||tool!=="line")return;const p=point(event.clientX,event.clientY,event.currentTarget);if(p)setEnd(p)}
 function finish(event:PointerEvent<HTMLDivElement>){if(!start||!end||tool!=="line")return;event.currentTarget.releasePointerCapture(event.pointerId);if(Math.hypot(end.x-start.x,end.y-start.y)>1){onChange([...notes,{x:start.x,y:start.y,x2:end.x,y2:end.y,text:draft.trim()}]);setDraft("")}setStart(null);setEnd(null)}
 return <section className="image-annotator"><div className="annotation-heading"><b>模型保留與圖片定位</b><span>線型燈可直接按住拖曳</span></div><div className="render-mode"><button className={mode==="strict"?"active":""} onClick={()=>onModeChange("strict")}>只做材質光影</button><button className={mode==="annotated"?"active":""} onClick={()=>onModeChange("annotated")}>僅標記位置可改</button><button className={mode==="redesign"?"active":""} onClick={()=>onModeChange("redesign")}>允許重新設計</button></div><div className="annotation-tools"><button className={tool==="line"?"active":""} onClick={()=>{setTool("line");setStart(null);setEnd(null)}}>↔ 拖曳線段</button><button className={tool==="point"?"active":""} onClick={()=>{setTool("point");setStart(null);setEnd(null)}}>● 單點標記</button></div><div className="annotation-entry"><input value={draft} onChange={e=>setDraft(e.target.value)} placeholder="例如：沿此線段配置 3000K 暖白 LED 燈條"/><span>{!draft.trim()?"先輸入備註":tool==="line"?(start?"拖曳中，放開完成":"在圖片按住並拖曳"):"請點位置"}</span></div><div className={`annotation-canvas ${draft.trim()?"is-placing":""}`} onClick={placePoint} onPointerDown={begin} onPointerMove={move} onPointerUp={finish}><img src={image} alt="定位備註用的空間圖片"/>{start&&end&&<span><em className="preview-line" style={{left:`${start.x}%`,top:`${start.y}%`,width:`${Math.hypot(end.x-start.x,end.y-start.y)}%`,transform:`rotate(${Math.atan2(end.y-start.y,end.x-start.x)*180/Math.PI}deg)`}}/><i className="pending" style={{left:`${start.x}%`,top:`${start.y}%`}}>起</i></span>}{notes.map((n,i)=>{const line=n.x2!==undefined&&n.y2!==undefined,dx=(n.x2??n.x)-n.x,dy=(n.y2??n.y)-n.y;return <span key={`${n.x}-${n.y}-${i}`}>{line&&<em style={{left:`${n.x}%`,top:`${n.y}%`,width:`${Math.hypot(dx,dy)}%`,transform:`rotate(${Math.atan2(dy,dx)*180/Math.PI}deg)`}}/>}<i style={{left:`${n.x}%`,top:`${n.y}%`}}>{i+1}</i>{line&&<i className="endpoint" style={{left:`${n.x2}%`,top:`${n.y2}%`}}>{i+1}</i>}</span>})}</div>{notes.length>0&&<ol>{notes.map((n,i)=><li key={`${n.x}-${n.y}-${i}`}><b>{i+1}</b><span>{n.text}<small>{n.x2!==undefined?`線段：(${n.x}%, ${n.y}%) → (${n.x2}%, ${n.y2}%)`:`單點：(${n.x}%, ${n.y}%)`}</small></span><button onClick={()=>onChange(notes.filter((_,j)=>j!==i))}>刪除</button></li>)}</ol>}</section>
}
