"use client";
import {MouseEvent,useState} from "react";
export type ImageNote={x:number;y:number;text:string};
export type RenderMode="strict"|"annotated"|"redesign";
export default function ImageAnnotator({image,notes,onChange,mode,onModeChange}:{image:string;notes:ImageNote[];onChange:(notes:ImageNote[])=>void;mode:RenderMode;onModeChange:(mode:RenderMode)=>void}){const[draft,setDraft]=useState("");function place(event:MouseEvent<HTMLDivElement>){if(!draft.trim())return;const box=event.currentTarget.getBoundingClientRect(),img=event.currentTarget.querySelector("img");if(!img)return;const scale=Math.min(box.width/img.naturalWidth,box.height/img.naturalHeight),width=img.naturalWidth*scale,height=img.naturalHeight*scale,left=box.left+(box.width-width)/2,top=box.top+(box.height-height)/2;if(event.clientX<left||event.clientX>left+width||event.clientY<top||event.clientY>top+height)return;const x=Math.round(((event.clientX-left)/width)*1000)/10,y=Math.round(((event.clientY-top)/height)*1000)/10;onChange([...notes,{x,y,text:draft.trim()}]);setDraft("")}return <section className="image-annotator"><div className="annotation-heading"><b>模型保留與圖片定位</b><span>設定允許 AI 改動的範圍</span></div><div className="render-mode"><button className={mode==="strict"?"active":""} onClick={()=>onModeChange("strict")}>只做材質光影</button><button className={mode==="annotated"?"active":""} onClick={()=>onModeChange("annotated")}>僅標記位置可改</button><button className={mode==="redesign"?"active":""} onClick={()=>onModeChange("redesign")}>允許重新設計</button></div><div className="annotation-entry"><input value={draft} onChange={e=>setDraft(e.target.value)} placeholder="例如：此處沿櫃體下緣配置暖白燈條"/><span>{draft.trim()?"請點圖片定位":"輸入後即可標記"}</span></div><div className={`annotation-canvas ${draft.trim()?"is-placing":""}`} onClick={place}><img src={image} alt="定位備註用的空間圖片"/>{notes.map((note,index)=><i key={`${note.x}-${note.y}-${index}`} style={{left:`${note.x}%`,top:`${note.y}%`}}>{index+1}</i>)}</div>{notes.length>0&&<ol>{notes.map((note,index)=><li key={`${note.x}-${note.y}-${index}`}><b>{index+1}</b><span>{note.text}<small>水平 {note.x}%、垂直 {note.y}%</small></span><button type="button" onClick={()=>onChange(notes.filter((_,i)=>i!==index))}>刪除</button></li>)}</ol>}</section>}
